[project]
name = "py-functional"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "assertpy>=1.1",
    "mock>=5.2.0",
    "pytest>=8.4.1",
    "pytest-cov>=6.2.1",
]
